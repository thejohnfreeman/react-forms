{
  "name": "@thejohnfreeman/react-forms-workspace",
  "private": true,
  "scripts": {
    "lint": "lerna run lint",
    "prepare": "lerna run prepare",
    "test": "lerna run test",
    "watch": "lerna run --parallel watch"
  },
  "devDependencies": {
    "lerna": "^3.10.7"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ]
}
